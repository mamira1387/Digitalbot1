# از یک ایمیج پایه PHP که شامل Apache هست استفاده کنید
FROM php:8.2-apache

# فایل‌های پروژه رو به دایرکتوری روت وب سرور Apache کپی کنید
# /var/www/html/ دایرکتوری پیش‌فرض برای فایل‌های وب در این ایمیج هست
COPY . /var/www/html/

# Apache به صورت پیش‌فرض روی پورت 80 گوش میده
# Render این پورت رو به صورت خودکار تشخیص میده
EXPOSE 80

# Apache به صورت خودکار با ایمیج پایه اجرا میشه
# نیازی به CMD یا ENTRYPOINT اضافی نیست، مگر اینکه تنظیمات خاصی داشته باشید
